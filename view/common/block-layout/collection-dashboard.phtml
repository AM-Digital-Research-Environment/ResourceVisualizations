<?php
/**
 * Collection Dashboard page block — the full set of visualizations.
 *
 * Loads the precomputed collection-overview.json and renders every chart using
 * the standard dashboard orchestrator with the complete `section` layout (the
 * JSON carries `resourceType: "section"`). The curated home-page subset lives in
 * the sibling "Collection Overview" block (collection-overview.phtml), which
 * loads the SAME JSON but overrides the layout via `data-layout`.
 *
 * @var \Laminas\View\Renderer\PhpRenderer $this
 * @var \Omeka\Api\Representation\SitePageBlockRepresentation $block
 */

// Dashboard front-end assets. This is a site-page block (Module.php does not
// run on page controllers), so request the CDN + core prelude too. Single
// source of truth: DreVisualizations\View\Helper\DashboardAssets.
$this->dashboardAssets(['cdn' => true]);

$siteSlug = $this->currentSite()->slug();
?>

<div class="resource-vis-block dashboard-block">
    <div class="dashboard-async-container"
         data-item-id="collection-overview"
         data-base-path="<?= $this->escapeHtmlAttr($this->basePath()) ?>"
         data-site-base="<?= $this->escapeHtmlAttr($this->basePath() . '/s/' . $siteSlug) ?>">
        <div class="rv-loading">
            <div class="rv-spinner"></div>
            <span><?= $this->translate('Loading collection dashboard…') ?></span>
        </div>
    </div>
</div>
