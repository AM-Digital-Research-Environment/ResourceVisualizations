<?php
/**
 * Podcasts page block.
 *
 * Analytics for the cluster's curated podcast episodes (item set 39095,
 * template 21, fabio:AudioDocument). Loads the precomputed podcasts.json and
 * renders via the standard dashboard orchestrator (resourceType 'podcasts' →
 * the podcasts layout). The headline chart is a word cloud built in the
 * precompute from the episodes' AI-generated transcripts (bibo:content).
 *
 * @var \Laminas\View\Renderer\PhpRenderer $this
 * @var \Omeka\Api\Representation\SitePageBlockRepresentation $block
 */

// Site-page block (Module.php does not run on page controllers), so request the
// CDN + core prelude too. Single source of truth: DashboardAssets view helper.
$this->dashboardAssets(['cdn' => true]);

$siteSlug = $this->currentSite()->slug();
?>

<div class="resource-vis-block dashboard-block">
    <div class="dashboard-async-container"
         data-item-id="podcasts"
         data-embed-slug="podcasts"
         data-base-path="<?= $this->escapeHtmlAttr($this->basePath()) ?>"
         data-site-base="<?= $this->escapeHtmlAttr($this->basePath() . '/s/' . $siteSlug) ?>">
        <div class="rv-loading">
            <div class="rv-spinner"></div>
            <span><?= $this->translate('Loading podcast analytics…') ?></span>
        </div>
    </div>
</div>
