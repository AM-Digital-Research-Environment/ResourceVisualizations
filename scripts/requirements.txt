# Precompute dependencies.
#
#   pip install -r scripts/requirements.txt
#
# pymysql   — direct MySQL connection (set DB_HOST to use it instead of the
#             default `docker compose exec`); lets the precompute run inside the
#             Omeka container or over a VPN.
# networkx  — Louvain community detection for the Discursive Communities graph
#             (PageRank is a pure-Python power iteration, so scipy is NOT needed).
PyMySQL>=1.1
networkx>=3.2
